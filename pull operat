the operations which are required for pull operation from github to git is given by:
git init for initilisation
clear which is used to clear the tab
git  remote add origin "https://github.com/venkisv12345/ram-12.git"
git pull origin master
